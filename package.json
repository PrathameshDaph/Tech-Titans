{
  "name": "eventflow-ai",
  "version": "2.0.0",
  "private": true,
  "description": "EventFlow AI — Mega-Event Hospitality & Mobility Digital Twin",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  }
}
